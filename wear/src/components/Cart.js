import React,{useState} from 'react'


const Cart = () => {
    const [cart, setCart] = useState({})
    const [subTotal, setSubTotal] = useState(0)
    
  return (
    <div>Cart</div>
  )
}

export default Cart